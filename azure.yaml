# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ZipDeploy-AzFunc
metadata:
  template: ZipDeploy-AzFunc@0.0.1-beta
infrastructure:
  provider: bicep
  path: deployment/infra
